import Head from "next/head";
import Image from "next/image";
import styles from "./styles.module.css";

const Home = (props) => {
  return (
    <div>
      <div className={styles.title}>Pure Css Animation Section</div>
    </div>
  );
};

export default Home;
